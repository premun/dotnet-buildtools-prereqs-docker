###
### DO NOT MODIFY THIS FILE!
###
### This YAML was auto-generated from PrPipelineTemplate
### To make changes, change the C# definition and rebuild its project
###

trigger: none

pr:
  branches:
    include:
    - main
  paths:
    include:
    - src/opensuse/*

resources:
  repositories:
  - repository: PublicVersionsRepo
    type: github
    name: dotnet/versions
    endpoint: dotnet

variables:
- template: variables/common.yml

- name: imageBuilder.pathArgs
  value: --path 'src/opensuse/*'

stages:
- template: ../common/templates/stages/dotnet/build-test-publish-repo.yml
  parameters:
    internalProjectName: ${{ variables.internalProjectName }}
    publicProjectName: ${{ variables.publicProjectName }}
